// SCENE: Logo System — Logo variations grid with usage guidelines
// Use when: Showing how the logo works across contexts (dark, light, icon-only).
// CONTENT to customize: logo images, guideline text
import { motion } from "framer-motion";
import { useInView } from "./useInView";

const ease = [0.22, 1, 0.36, 1];

export default function LogoSystem() {
  const { ref, isInView } = useInView(0.3);

  /* CONTENT: Replace with client-specific guidelines */
  const guidelines = [
    { label: "Clear Space", desc: "Minimum padding equal to the height of the lowercase 'e' on all sides." },
    { label: "Minimum Size", desc: "32px for digital applications, 10mm for print." },
    { label: "Icon Only", desc: "The icon may be used standalone when the full wordmark has been established in context." },
    { label: "No Distortion", desc: "Never stretch, rotate, skew, or alter the proportions of the mark." },
  ];

  return (
    <section ref={ref} className="flow-section relative px-8">
      <div className="max-w-5xl w-full relative z-10">
        <motion.p className="font-sans text-xs tracking-[0.4em] uppercase text-primary/70 font-semibold mb-16 text-center"
          initial={{ opacity: 0 }} animate={isInView ? { opacity: 1 } : {}}
          transition={{ duration: 0.8, delay: 0.2 }}>Logo System</motion.p>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 mb-16">
          {/* CONTENT: Dark background logo variant */}
          <motion.div className="border border-border/15 rounded-2xl p-12 bg-card/5 flex items-center justify-center min-h-[200px]"
            initial={{ opacity: 0, y: 30 }} animate={isInView ? { opacity: 1, y: 0 } : {}}
            transition={{ duration: 1, delay: 0.4, ease }} style={{ perspective: 800 }}>
            <span className="font-display text-3xl font-black text-foreground/90">brandname <span className="font-light text-foreground/40">descriptor</span></span>
          </motion.div>
          {/* CONTENT: Light background logo variant */}
          <motion.div className="border border-border/15 rounded-2xl p-12 flex items-center justify-center min-h-[200px]"
            style={{ background: "hsl(0, 0%, 95%)" }}
            initial={{ opacity: 0, y: 30 }} animate={isInView ? { opacity: 1, y: 0 } : {}}
            transition={{ duration: 1, delay: 0.6, ease }} >
            <span className="font-display text-3xl font-black" style={{ color: "hsl(252,10%,15%)" }}>brandname <span className="font-light" style={{ color: "hsl(252,10%,50%)" }}>descriptor</span></span>
          </motion.div>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
          {guidelines.map((g, i) => (
            <motion.div key={i} className="border border-border/15 rounded-xl p-5 bg-card/5 backdrop-blur-sm"
              initial={{ opacity: 0, y: 20 }}
              animate={isInView ? { opacity: 1, y: 0 } : {}}
              transition={{ duration: 0.7, delay: 0.8 + i * 0.1, ease }}>
              <h4 className="font-sans text-xs font-bold text-foreground/70 uppercase tracking-wider mb-2">{g.label}</h4>
              <p className="font-sans text-xs text-muted-foreground/40 leading-relaxed font-light">{g.desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
