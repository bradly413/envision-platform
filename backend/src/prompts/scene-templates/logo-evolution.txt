// SCENE: Logo Evolution — Before/after comparison with feature cards
// Use when: Showing the transformation from old to new identity.
// CONTENT to customize: before/after brand display, feature cards
import { motion } from "framer-motion";
import { useInView } from "./useInView";

const ease = [0.22, 1, 0.36, 1];

export default function LogoEvolution() {
  const { ref, isInView } = useInView(0.3);

  /* CONTENT: Replace with client-specific evolution features */
  const features = [
    { label: "Lowercase", desc: "Approachable, modern, and human — removing the distance that uppercase creates." },
    { label: "Heavy weight", desc: "Permanence and confidence. A mark that holds space and commands recognition." },
    { label: "Tech-forward", desc: "Geometric precision with Poppins — clean, systematic, and digitally native." },
  ];

  return (
    <section ref={ref} className="flow-section relative px-8">
      <motion.div className="absolute left-0 right-0 h-px pointer-events-none"
        style={{ background: "linear-gradient(90deg, transparent, var(--brand-primary-alpha, hsl(203, 72%, 38%, 0.08)), transparent)" }}
        animate={isInView ? { top: ["20%", "80%", "20%"] } : {}}
        transition={{ duration: 15, repeat: Infinity, ease: "easeInOut" }} />
      <div className="max-w-5xl w-full relative z-10">
        <motion.p className="font-sans text-xs tracking-[0.4em] uppercase text-primary/70 font-semibold mb-16 text-center"
          initial={{ opacity: 0, y: -15 }} animate={isInView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 1, delay: 0.2 }}>Logo Evolution</motion.p>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-16 items-center mb-20">
          <motion.div className="text-center perspective-container"
            initial={{ opacity: 0, x: -40, rotateY: -15 }}
            animate={isInView ? { opacity: 1, x: 0, rotateY: 0 } : {}}
            transition={{ duration: 1.2, delay: 0.4, ease }}>
            {/* CONTENT: Replace with old brand display */}
            <div className="border border-border/15 rounded-xl p-10 bg-card/5">
              <span className="font-display text-2xl md:text-3xl tracking-[0.4em] uppercase text-muted-foreground/25 font-light">BEFORE</span>
            </div>
            <p className="mt-3 text-xs text-muted-foreground/30 uppercase tracking-widest">Previous</p>
          </motion.div>
          <motion.div className="text-center"
            initial={{ opacity: 0, x: 40 }}
            animate={isInView ? { opacity: 1, x: 0 } : {}}
            transition={{ duration: 1.2, delay: 0.8, ease }}>
            {/* CONTENT: Replace with new brand display */}
            <div className="border border-primary/20 rounded-xl p-10 bg-primary/5">
              <span className="font-display text-3xl md:text-4xl font-black text-foreground">after</span>
            </div>
            <motion.p className="mt-3 text-xs uppercase tracking-widest"
              style={{ color: "var(--brand-primary)" }}
              animate={isInView ? { opacity: [0.4, 1, 0.4] } : {}}
              transition={{ duration: 3, repeat: Infinity }}>Evolved</motion.p>
          </motion.div>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {features.map((f, i) => (
            <motion.div key={i} className="border border-border/15 rounded-xl p-6 bg-card/5 backdrop-blur-sm"
              initial={{ opacity: 0, y: 30 }}
              animate={isInView ? { opacity: 1, y: 0 } : {}}
              transition={{ duration: 0.8, delay: 1.4 + i * 0.15, ease }}>
              <h3 className="font-sans text-sm font-bold text-foreground/80 mb-2">{f.label}</h3>
              <p className="font-sans text-xs text-muted-foreground/40 leading-relaxed font-light">{f.desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
