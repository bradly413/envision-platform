// SCENE: Brand Context — Two-column layout with current identity + challenge points
// Use when: Framing the problem or current state before showing the solution.
// CONTENT to customize: current brand display, challenge bullet points
import { motion } from "framer-motion";
import { useInView } from "./useInView";

const ease = [0.22, 1, 0.36, 1];

export default function BrandContext() {
  const { ref, isInView } = useInView(0.3);

  /* CONTENT: Replace challenges array with client-specific points */
  const challenges = [
    "The current mark feels thin, impermanent, and forgettable.",
    "Uppercase letterforms create distance rather than connection.",
    "Minimal execution reads as unfinished rather than refined.",
    "The brand needs weight, warmth, and strategic presence.",
  ];

  return (
    <section ref={ref} className="flow-section relative px-8 md:px-16">
      {[...Array(3)].map((_, i) => (
        <motion.div key={i} className="absolute h-px pointer-events-none"
          style={{ width: 80 + i * 40, top: `${25 + i * 22}%`,
            left: i % 2 === 0 ? "5%" : undefined, right: i % 2 === 1 ? "5%" : undefined,
            background: `linear-gradient(90deg, transparent, var(--brand-primary-alpha, hsl(203, 72%, 38%, ${0.06 + i * 0.02})), transparent)` }}
          initial={{ scaleX: 0 }} animate={isInView ? { scaleX: 1 } : {}}
          transition={{ duration: 1.5, delay: 0.3 + i * 0.3, ease }} />
      ))}
      <div className="max-w-6xl w-full grid grid-cols-1 md:grid-cols-2 gap-16 items-center relative z-10">
        <motion.div className="flex flex-col items-center justify-center perspective-container"
          initial={{ opacity: 0, x: -60, rotateY: -20 }}
          animate={isInView ? { opacity: 1, x: 0, rotateY: 0 } : {}}
          transition={{ duration: 1.4, delay: 0.3, ease }}>
          <motion.div className="border border-border/20 rounded-xl p-12 md:p-16 bg-card/10 backdrop-blur-sm w-full flex flex-col items-center"
            whileHover={{ rotateY: 5, scale: 1.02 }} transition={{ duration: 0.5 }}>
            {/* CONTENT: Replace with client's current brand display */}
            <span className="font-display text-3xl md:text-4xl tracking-[0.5em] uppercase text-muted-foreground/30 font-light">BRAND</span>
            <span className="mt-2 font-sans text-xs tracking-[0.3em] uppercase text-muted-foreground/15">Name</span>
          </motion.div>
          <motion.p className="mt-4 font-sans text-xs tracking-widest uppercase text-muted-foreground/20"
            initial={{ opacity: 0, y: 10 }} animate={isInView ? { opacity: 1, y: 0 } : {}}
            transition={{ duration: 0.8, delay: 1.2 }}>Current Identity</motion.p>
        </motion.div>
        <div className="space-y-8">
          <motion.div className="flex items-center gap-3"
            initial={{ opacity: 0, x: 30 }} animate={isInView ? { opacity: 1, x: 0 } : {}}
            transition={{ duration: 0.8, delay: 0.5 }}>
            <motion.div className="h-px flex-1 max-w-[40px]"
              style={{ transformOrigin: "left", background: "var(--brand-primary)" }}
              initial={{ scaleX: 0 }} animate={isInView ? { scaleX: 1 } : {}}
              transition={{ duration: 0.8, delay: 0.7 }} />
            <p className="font-sans text-xs tracking-[0.3em] uppercase text-primary font-semibold">The Challenge</p>
          </motion.div>
          {challenges.map((text, i) => (
            <motion.p key={i} className="font-sans text-lg md:text-xl text-foreground/55 leading-relaxed font-light"
              initial={{ opacity: 0, y: 30, filter: "blur(4px)" }}
              animate={isInView ? { opacity: 1, y: 0, filter: "blur(0px)" } : {}}
              transition={{ duration: 0.9, delay: 0.8 + i * 0.2, ease }}>{text}</motion.p>
          ))}
        </div>
      </div>
    </section>
  );
}
