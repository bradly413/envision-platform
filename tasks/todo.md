# Envision Platform — Task Queue

## Current Sprint
- [x] Critical bug hunt 2026-08-04: reject truncated AI JSON fragments as structured builds

## Backlog
- [ ] Surface provider truncation (`stop_reason` / `finish_reason`) into builder repair path
- [ ] Bound builder media `readAsDataURL` size
- [ ] Align `schema.sql` with `portals.plain_password` usage (or stop writing plaintext)

## Completed
- [x] Initial Claude Code setup
