export default function CalendarPage() {
  return (
    <div style={{ padding: 32, fontFamily: 'Inter, sans-serif' }}>
      <h1 style={{ fontSize: 24, fontWeight: 800, color: '#111827', margin: '0 0 8px' }}>Calendar</h1>
      <p style={{ color: '#6B7280', fontSize: 14 }}>Wire up Google Calendar OAuth here — use the same flow from the Cornell Hub as a reference. The backend Google auth route is stubbed in <code>routes/calendar.js</code>.</p>
    </div>
  );
}
