// SCENE: Closing — Final CTA with breathing gradient and brand sign-off
// Use when: Last scene. Resolves the emotional arc.
// CONTENT to customize: brand name, tagline, footer text
import { motion } from "framer-motion";
import { useInView } from "./useInView";

const ease = [0.22, 1, 0.36, 1];

export default function Closing() {
  const { ref, isInView } = useInView(0.3);

  return (
    <section ref={ref} className="flow-section relative overflow-hidden">
      <motion.div className="absolute inset-0 pointer-events-none"
        style={{ background: "radial-gradient(circle at 50% 50%, var(--brand-primary-alpha, hsl(203, 72%, 38%, 0.06)), transparent 60%)" }}
        animate={isInView ? { scale: [1, 1.1, 1], opacity: [0.5, 0.8, 0.5] } : {}}
        transition={{ duration: 5, repeat: Infinity, ease: "easeInOut" }} />
      <div className="relative z-10 flex flex-col items-center px-8">
        <div className="overflow-hidden perspective-container">
          {/* CONTENT: Replace brand name */}
          <motion.h2 className="font-display text-5xl md:text-7xl lg:text-8xl font-black text-center"
            initial={{ y: 100, opacity: 0, rotateX: 45 }}
            animate={isInView ? { y: 0, opacity: 1, rotateX: 0 } : {}}
            transition={{ duration: 1.5, delay: 1, ease }}
            style={{ transformOrigin: "bottom center" }}>
            <span className="text-foreground">brandname</span>
            <span className="text-foreground/40 font-light ml-4">descriptor</span>
          </motion.h2>
        </div>
        {/* CONTENT: Replace tagline */}
        <motion.p className="mt-8 font-sans text-sm md:text-base uppercase font-light text-center"
          style={{ color: "var(--brand-primary)" }}
          initial={{ opacity: 0, filter: "blur(8px)", letterSpacing: "0.8em" }}
          animate={isInView ? { opacity: 1, filter: "blur(0px)", letterSpacing: "0.3em" } : {}}
          transition={{ duration: 2, delay: 1.6, ease }}>
          See further. Build bolder.
        </motion.p>
        <motion.div className="mt-10 h-[1px] rounded-full"
          style={{ background: "linear-gradient(90deg, transparent, var(--brand-primary-alpha, hsl(203, 72%, 38%, 0.3)), transparent)" }}
          initial={{ width: 0 }} animate={isInView ? { width: 100 } : {}}
          transition={{ duration: 1.5, delay: 2.2, ease }} />
        {/* CONTENT: Replace footer text */}
        <motion.p className="mt-8 font-sans text-xs text-muted-foreground/20 tracking-widest uppercase"
          initial={{ opacity: 0 }} animate={isInView ? { opacity: 1 } : {}}
          transition={{ duration: 1, delay: 2.8 }}>
          Brand Identity System — 2026
        </motion.p>
      </div>
    </section>
  );
}
