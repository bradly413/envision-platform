// SCENE: Wordmark Reveal — Brand name with dramatic blur/scale entrance
// Use when: Introducing the brand identity after the opening.
// CONTENT to customize: brand name words, tagline, glow color
import { motion } from "framer-motion";
import { useInView } from "./useInView";

const ease = [0.16, 1, 0.3, 1];

export default function WordmarkReveal() {
  const { ref, isInView } = useInView(0.3);

  return (
    <section ref={ref} className="flow-section relative overflow-hidden">
      <div className="relative z-10 flex flex-col items-center">
        <div className="flex items-baseline gap-4">
          {/* CONTENT: Replace brand name words */}
          <motion.span className="font-display text-5xl md:text-7xl lg:text-8xl font-black text-foreground"
            style={{ textShadow: "0 0 40px var(--brand-primary-glow, hsl(203, 72%, 38%, 0.3))" }}
            initial={{ opacity: 0, y: 40, scale: 0.8, filter: "blur(12px)" }}
            animate={isInView ? { opacity: 1, y: 0, scale: 1, filter: "blur(0px)" } : {}}
            transition={{ duration: 2.5, delay: 0.5, ease }} >
            /* CONTENT: Primary word */
            brandname
          </motion.span>
          <motion.span className="font-display text-5xl md:text-7xl lg:text-8xl font-extralight text-foreground/60"
            style={{ textShadow: "0 0 30px var(--brand-primary-glow, hsl(203, 72%, 38%, 0.15))" }}
            initial={{ opacity: 0, y: 40, scale: 0.8, filter: "blur(12px)" }}
            animate={isInView ? { opacity: 1, y: 0, scale: 1, filter: "blur(0px)" } : {}}
            transition={{ duration: 2.5, delay: 1.8, ease }} >
            /* CONTENT: Secondary word */
            descriptor
          </motion.span>
        </div>
        <motion.div className="mt-6 h-[2px] rounded-full"
          style={{ background: "linear-gradient(90deg, transparent, var(--brand-primary), var(--brand-accent), transparent)" }}
          initial={{ width: 0, opacity: 0 }}
          animate={isInView ? { width: 280, opacity: 1 } : {}}
          transition={{ duration: 2, delay: 3, ease }} />
        <motion.p className="mt-8 font-sans text-xs md:text-sm uppercase text-muted-foreground/30 font-light"
          initial={{ opacity: 0, letterSpacing: "0.3em" }}
          animate={isInView ? { opacity: 1, letterSpacing: "0.5em" } : {}}
          transition={{ duration: 2.5, delay: 3.8, ease }} >
          {/* CONTENT: Replace tagline */}
          See further. Build bolder.
        </motion.p>
      </div>
    </section>
  );
}
